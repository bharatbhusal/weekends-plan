# Core Application URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database Configuration (MongoDB Atlas M0 Shared Cluster)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.abc12.mongodb.net/events_db?retryWrites=true&w=majority

# Revalidation Secret for ISR Cache
REVALIDATION_SECRET=generate_a_long_random_alphanumeric_string_here

# Luma API (no auth needed, but city config is managed in config/sources.ts)
# See config/sources.ts for city coverage settings
